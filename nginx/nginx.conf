# Generated by nginxconfig.io
# https://www.digitalocean.com/community/tools/nginx?domains.0.server.domain=cabinetkhalouiauguste.fr&domains.0.server.path=%2Fvar%2Fwww%2Fhtml&domains.0.server.documentRoot=&domains.0.server.wwwSubdomain=true&domains.0.https.http3=true&domains.0.https.certType=custom&domains.0.https.sslCertificate=%2Fcerts%2Ffullchain1.pem&domains.0.https.sslCertificateKey=%2Fcerts%2Fprivkey1.pem&domains.0.php.phpServer=127.0.0.1%3A9000&domains.0.php.wordPressRules=true&global.security.referrerPolicy=no-referrer&global.security.contentSecurityPolicy=default-src%20%27self%27%3B%20img-src%20%27self%27%20data%3A%20http%3A%20https%3A%20%2A.gravatar.com%3B%20script-src%20%27self%27%20%27unsafe-inline%27%20%27unsafe-eval%27%3B%20style-src%20%27self%27%20%27unsafe-inline%27%20http%3A%20https%3A%20fonts.googleapis.com%3B%20font-src%20%27self%27%20data%3A%20http%3A%20https%3A%20fonts.googleapis.com&global.security.limitReq=true&global.nginx.user=nginx&global.nginx.pid=%2Fvar%2Frun%2Fnginx.pid&global.docker.dockerCompose=true

user                 nginx;
pid                  /var/run/nginx.pid;
worker_processes     auto;
worker_rlimit_nofile 65535;

# Load modules
include              /etc/nginx/modules-enabled/*.conf;

events {
    multi_accept       on;
    worker_connections 65535;
}

http {
    charset                utf-8;
    sendfile               on;
    tcp_nopush             on;
    tcp_nodelay            on;
    server_tokens          off;
    log_not_found          off;
    types_hash_max_size    2048;
    types_hash_bucket_size 64;
    client_max_body_size   16M;

    # MIME
    include                mime.types;
    default_type           application/octet-stream;

    # Logging
    access_log             /var/log/nginx/access.log;
    error_log              /var/log/nginx/error.log warn;

    # Limits
    limit_req_log_level    warn;
    limit_req_zone         $binary_remote_addr zone=login:10m rate=10r/m;
    limit_req_zone         $binary_remote_addr zone=admin:10m rate=50r/m;

    # SSL
    ssl_session_timeout    1d;
    ssl_session_cache      shared:SSL:10m;
    ssl_session_tickets    off;

    # Diffie-Hellman parameter for DHE ciphersuites
    #ssl_dhparam            /etc/nginx/dhparam.pem;

    # Mozilla Intermediate configuration
    ssl_protocols          TLSv1.2 TLSv1.3;
    ssl_ciphers            ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

    # OCSP Stapling
    ssl_stapling           on;
    ssl_stapling_verify    on;
    resolver               1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4 208.67.222.222 208.67.220.220 valid=60s;
    resolver_timeout       2s;

    # Load configs
    include                /etc/nginx/conf.d/*.conf;
    include                /etc/nginx/sites-enabled/*;
}